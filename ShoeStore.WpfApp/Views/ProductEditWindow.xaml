<Window x:Class="ShoeStore.WpfApp.Views.ProductEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование товара" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Icon="/Assets/Icon.ico"
        Background="{StaticResource MainBackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Данные товара" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Артикул (выбор из существующих или новый) -->
                <GroupBox Header="Артикул" Margin="0,5">
                    <StackPanel>
                        <ComboBox x:Name="ArticleComboBox" DisplayMemberPath="Title" IsEditable="True"
                                  ItemsSource="{Binding Articles}"/>
                    </StackPanel>
                </GroupBox>

                <!-- Основные поля -->
                <GroupBox Header="Основное" Margin="0,5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Наименование:" Margin="5"/>
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="NameTextBox" Margin="5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Описание:" Margin="5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" x:Name="DescriptionTextBox" AcceptsReturn="True" Height="60" Margin="5"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Фото (путь):" Margin="5"/>
                        <TextBox Grid.Row="2" Grid.Column="1" x:Name="PhotoPathTextBox" Margin="5"/>
                    </Grid>
                </GroupBox>

                <!-- Справочники -->
                <GroupBox Header="Справочники" Margin="0,5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Категория:" Margin="5"/>
                        <ComboBox Grid.Row="0" Grid.Column="1" x:Name="CategoryComboBox" DisplayMemberPath="Name" ItemsSource="{Binding Categories}" Margin="5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Производитель:" Margin="5"/>
                        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="ManufacturerComboBox" DisplayMemberPath="Name" ItemsSource="{Binding Manufacturers}" Margin="5"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Поставщик:" Margin="5"/>
                        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="SupplierComboBox" DisplayMemberPath="Name" ItemsSource="{Binding Suppliers}" Margin="5"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Единица измерения:" Margin="5"/>
                        <ComboBox Grid.Row="3" Grid.Column="1" x:Name="UnitComboBox" DisplayMemberPath="Name" ItemsSource="{Binding Units}" Margin="5"/>
                    </Grid>
                </GroupBox>

                <!-- Цены и количество -->
                <GroupBox Header="Цены и наличие" Margin="0,5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Цена:" Margin="5"/>
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="PriceTextBox" Margin="5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Скидка %:" Margin="5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" x:Name="DiscountTextBox" Margin="5"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Количество:" Margin="5"/>
                        <TextBox Grid.Row="2" Grid.Column="1" x:Name="QuantityTextBox" Margin="5"/>
                    </Grid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Сохранить" Width="100" Margin="5" Click="Save_Click"/>
            <Button Content="Отмена" Width="100" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>